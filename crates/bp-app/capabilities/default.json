{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "BoxPassword 기본 권한 — main과 quick 윈도우 모두에 코어 기능 + 백업 다이얼로그 허용",
  "windows": ["main", "quick"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-save",
    "dialog:allow-open",
    "notification:default"
  ]
}
